# Frontend App

React + TypeScript + Vite app generated for a Reikujo `frontend-vite` project.

## Commands

```bash
pnpm install
pnpm dev
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

Update this file after the real app stack, routing, styling, and test setup are finalized.
